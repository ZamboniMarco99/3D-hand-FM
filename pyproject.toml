[project]
name = "3d-hand-fm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "hydra-core>=1.3.2",
    "lightning>=2.4.0",
    "torch>=2.4.1",
    "torchaudio>=2.4.1",
    "torchvision>=0.19.1",
    "wandb>=0.12.10",
]

[tool.ruff.lint]
select = ["ALL"]
# Avoid enforcing print rule and imperative docstrings
ignore = ["T201", "D401"]

[tool.ruff]
line-length = 120
