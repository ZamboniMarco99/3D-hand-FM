[project]
name = "3d-hand-fm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "lightning>=2.4.0",
    "torch>=2.4.1",
    "torchaudio>=2.4.1",
    "torchvision>=0.19.1",
]

[tool.ruff.lint]
select = ["ALL"]
# Avoid enforcing print rule
ignore = ["T201"]

[tool.ruff]
line-length = 120
